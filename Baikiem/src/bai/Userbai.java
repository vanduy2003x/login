/*
package bai;

import java.util.ArrayList;
import java.util.Scanner;

public class Userbai {
    public Username register(Scanner scanner, ArrayList<Username> usernames) {
        boolean rue = true;
        String name = "", pass = "", email = "";
        while (rue) {
            System.out.println("Mời bạn nhập tên đăng nhập");
            name = scanner.nextLine();
            System.out.println("Mời bạn nhập mật khẩu");
            pass = scanner.nextLine();
            System.out.println("Mời bạn nhập emmail");
            email = scanner.nextLine();

        }
    }

    public String Login(Scanner scanner, ArrayList<Username> usernames) {
        scanner.nextLine();
        System.out.println("Mời bạn nhập tên đăng nhập");
        String name = scanner.nextLine();
        System.out.println("Mời bạn nhập mật khẩu");
        String pass = scanner.nextLine();
        for (Username username : usernames) {
            if (username.getUsername().equals(name) && username.getPasswork().equals(pass)) {

            }
        }
    }

}*/
